<style>
    .container {
        width: 800px;
        justify-content: center;
        align-items: center;
        align-content: center;
        display: flex;
        flex-direction: column;
    }

    .value {
        padding: 10px;
        margin: 5px;
        background: #e23e57;
        color: #fff;
        border-radius: 100%;
        width: 15px;
        text-align: center;
        text-decoration: none;
    }

    .level {
        display: flex;
        margin: 10px;
        justify-content: space-between;
        width: 350px;
    }

    .answers {
        display: flex;
        justify-content: space-between;

    }

    .level span {
        font-size: 50px;
    }
</style>
<div class="container">
    <label>Hey <strong>{{name}}</strong>! How's it going?</label>

    <h3>{{title}}</h3>
    <br>
    <strong>{{description}}</strong>

    <div class="level">
        <span>👍</span>
        <span>👎</span>
    </div>

    <div class="answers">
        <a href="{{link}}/1?u={{user_id}}" class="value">1</a>
        <a href="{{link}}/2?u={{user_id}}" class="value">2</a>
        <a href="{{link}}/3?u={{user_id}}" class="value">3</a>
        <a href="{{link}}/4?u={{user_id}}" class="value">4</a>
        <a href="{{link}}/5?u={{user_id}}" class="value">5</a>
        <a href="{{link}}/6?u={{user_id}}" class="value">6</a>
        <a href="{{link}}/7?u={{user_id}}" class="value">7</a>
        <a href="{{link}}/8?u={{user_id}}" class="value">8</a>
        <a href="{{link}}/9?u={{user_id}}" class="value">9</a>
        <a href="{{link}}/10?u={{user_id}}" class="value">10</a>
    </div>

    <br>
    <br>

    <img src="https://media.giphy.com/media/mCRJDo24UvJMA/giphy.gif" alt="this slowpoke moves" width=250 />
    <strong>Your opinion helps a lot</strong>
    <h3>Sidney Lopes | <strong>NLW</strong></h3>
</div>